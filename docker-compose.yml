services:
  app:
    platform: linux/x86_64
    build:
      context: .
      target: local
    volumes:
      - .:/app
      - ~/documents/dev/repos/:/root/repos
      - ~/.config/gcloud:/root/.config/gcloud
